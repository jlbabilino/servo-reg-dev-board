[package]
name = "shared_types"
version = "0.1.0"
edition = "2024"

[dependencies]
defmt = { version = "1.1.1", optional = true }
postcard-bindgen = { version = "0.7.1", optional = true }
serde = { version = "1.0.228", default-features = false }

[features]
default = []
defmt = ["dep:defmt"]
postcard-bindgen = ["dep:postcard-bindgen"]
